# Montunos WL Bot (Render)

Comandos:
- /wlpass id:<DiscordID>
- /wldenied id:<DiscordID>
- /wdpass id:<DiscordID>
- /wddenied id:<DiscordID>

## Deploy en Render (Free)
Servicio recomendado en free: **Web Service** (Background Worker no está disponible en free).

Build Command: `npm install`
Start Command: `npm start`

Ruta para ping: `/health`

Configura las variables de entorno desde `.env.example`.
